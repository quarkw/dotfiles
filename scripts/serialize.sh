#!/bin/bash
# A general utility to copy (dot)files with some processing
# Git directories will not be copied directly
# Instead, the following information will be recorded:
# Upstream, current branch, current tag, current commit
# If the repo is unclean, a patch file will be generated
